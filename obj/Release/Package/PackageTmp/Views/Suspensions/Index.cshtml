@model IEnumerable<PruebaBestfacar.Models.Suspension>

@{
    ViewBag.Tittle = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vehiculo.Marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Delantera)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trasera)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vehiculo.Marca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Delantera)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trasera)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.SuspensionID }) |
                @Html.ActionLink("Details", "Details", new { id=item.SuspensionID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.SuspensionID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
