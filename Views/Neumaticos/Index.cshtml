@model IEnumerable<PruebaBestfacar.Models.Neumaticos>

@{
    ViewBag.Tittle = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vehiculo.Marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoAros)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vehiculo.Marca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoAros)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.NeumaticosID }) |
                @Html.ActionLink("Details", "Details", new { id=item.NeumaticosID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.NeumaticosID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
